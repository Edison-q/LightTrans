# Python
__pycache__/
*.pyc
.venv/

# Runtime data (privacy - never commit these)
history.json
token.txt
server.log
接收文件/
icon.png

# TLS private keys (never commit; public certs are fine)
server.key
ca.key

# OS
.DS_Store
Thumbs.db
history.backup.json
